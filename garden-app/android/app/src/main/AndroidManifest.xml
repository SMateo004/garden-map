<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Garden"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- El intent-filter MAIN/LAUNCHER vive en los activity-alias de abajo
                 (.IconDefault / .IconVariantB), no acá — así el ícono mostrado en el
                 launcher depende de cuál alias está enabled, en vez de fijarse siempre
                 al ic_launcher de esta Activity real. -->
        </activity>
        <!-- Icono estacional en runtime — mecanismo real de Android: cada variante es un
             activity-alias que apunta a .MainActivity con su propio android:icon. Solo se
             puede alternar ENTRE variantes que ya están declaradas acá y empaquetadas en
             este APK/AAB — agregar una variante nueva (arte real de temporada) requiere un
             build nuevo + una nueva publicación en Play Store, no hay forma de evitarlo.
             Se activa/desactiva por código con
             PackageManager.setComponentEnabledSetting (ver IconSwitcher.kt) — exactamente
             un alias debe estar enabled a la vez, el resto disabled, o el launcher del
             usuario muestra íconos duplicados o ninguno.
             "VariantB" ahora mismo es un PLACEHOLDER: mismo arte adaptativo que el ícono
             default (ic_launcher_variant_b.xml duplica ic_launcher.xml) — solo prueba que
             el mecanismo end-to-end funciona, no es arte estacional real todavía.
             NOTA: según el lanzador (launcher) del teléfono, el ícono nuevo puede tardar en
             refrescarse hasta que se reinicie la app o el launcher — no siempre es
             instantáneo como en iOS. -->
        <activity-alias
            android:name=".IconDefault"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".IconVariantB"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_variant_b"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- Facebook Login -->
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>
        <meta-data android:name="com.facebook.sdk.ClientToken" android:value="@string/facebook_client_token"/>
        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Foreground service: mantiene vivo el envío de ubicación GPS
             durante un paseo activo aunque el cuidador minimice la app,
             bloquee la pantalla, O CIERRE LA APP POR COMPLETO (swipe desde
             apps recientes) — mismo patrón que el "modo conductor" de Uber.
             Ver GpsTrackingSession/GpsTaskHandler. Requiere declarar
             foregroundServiceType="location" desde Android 14 (API 34).

             stopWithTask="false" es la parte crítica de este comportamiento:
             el default de Android es matar el servicio apenas se remueve la
             app de "recientes", lo cual anulaba por completo el propósito
             del foreground service (el mismo bug que Uber evita con este
             mismo flag). Bug real reportado: la ubicación dejaba de
             transmitirse en cuanto el cuidador cerraba la app del todo. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:stopWithTask="false"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Necesario para que el tracking GPS del foreground service siga
         entregando ubicación mientras la app está minimizada/en background
         durante un paseo activo (ver GpsTrackingSession). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Galería en Android ≤ 12 — image_picker usa ACTION_GET_CONTENT/PICK ahí,
         que solo entrega la URI de la foto elegida, no lectura amplia. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- No se declara READ_MEDIA_IMAGES (Android 13+): image_picker_android
         0.8.13+ usa el Photo Picker nativo (ACTION_PICK_IMAGES), que entrega
         solo la foto elegida sin necesidad de lectura amplia de galería —
         todos los usos en la app son selección de una sola foto. `gal`
         (guardar el QR en galería) solo escribe, no lee. Estaba declarado
         de más antes; Play Console lo marcó como permiso sin uso real. -->

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
