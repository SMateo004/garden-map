# API base URL: debe coincidir con el puerto del backend (por defecto 3000)
VITE_API_URL=http://localhost:3000

# Optional: Stripe publishable key for future payment UI
# VITE_STRIPE_PUBLISHABLE_KEY=
